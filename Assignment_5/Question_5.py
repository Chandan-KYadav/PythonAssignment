# Write a python script which takes athree digit number from the user and display only its first digit. 

userInput = int(input('Enter three digit number'))

use_operator = userInput // 100

print(f'Output is {use_operator}')