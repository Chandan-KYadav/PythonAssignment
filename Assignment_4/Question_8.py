# Write a python script to calculate simple interst

principle = eval(input('Enter principle amount '))

interest = eval(input('Enter interest '))

time = eval(input('Enter time '))

simpleInterest = (principle * interest * time) / 100 

print(simpleInterest)