# Write a python script to calculate area of rectangle. 

length = eval(input('enter length of rectangle'))
width = eval(input('enter width of rectangle'))

area_of_rectangle = length * width 

print(area_of_rectangle)