# Write a python scipt to calculate the area of triangle, Number is entered by users. 

base_of_triangle = eval(input('Enter base of a triangle'))

height_of_triangle = eval(input('Enter height of a triangle'))

area_of_triangle = 0.5 * base_of_triangle * height_of_triangle

print(area_of_triangle)