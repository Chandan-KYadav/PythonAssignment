# Write a python script to find x power y, where values of x and y are given by user. 

num = eval(input('Enter the number. '))

power = eval(input('Enter the power. '))

use_operator = num ** power

print(f'Output is {use_operator}')

